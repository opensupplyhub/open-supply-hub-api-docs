version: "3.9"

services:
  swagger-ui:
    image: nginx:latest
    ports:
      - 8090:80
    volumes:
      - ./spec:/usr/share/nginx/html

  app:
    build: ./app
    ports:
      - 8095:80
    volumes:
      - ./app:/code/app
