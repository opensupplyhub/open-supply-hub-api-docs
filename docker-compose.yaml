version: "3.9"

services:
  swagger-ui:
    image: swaggerapi/swagger-ui:v5.11.7
    volumes:
      - ./openapi.yaml:/var/www/app/openapi.yaml
    ports:
      - 8090:8080
    environment:
      SWAGGER_JSON: /var/www/app/openapi.yaml
