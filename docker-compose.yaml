version: "3.9"

services:
  # API Documentation
  swagger-ui:
    image: swaggerapi/swagger-ui:latest
    volumes:
      - ./docs.yaml:/var/www/app/docs.yaml
    ports:
      - 8090:8080
    environment:
      SWAGGER_JSON: /var/www/app/docs.yaml
