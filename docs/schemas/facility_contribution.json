{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "description": "This schema represents a facility",
  "allOf": [
    {
      "properties": {
        "os_id": {
          "type": "string",
          "description": "The unique identifier for a facility"
        },
        "contribution_type": {
          "type": "string",
          "description": "The type of contribution was made to the facility",
          "enum": ["create", "update"]
        },
        "user_id": {
          "type": "string",
          "description": "The unique identifier for a user"
        }
      },
      "required": ["os_id", "contribution_type", "user_id"]
    },
    {
      "$ref": "facility_base.json"
    }
  ]
}
